<script>
/**
* 应用级别的配置，供所有页面公用
*/
import util from './util'
import { connectApp } from './Common/js/redux'
import store from './store'

export default connectApp(store, {
  showMenu: util.showMenu,
  createShortcut: util.createShortcut
})
</script>
